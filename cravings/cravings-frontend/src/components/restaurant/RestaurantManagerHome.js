import { Typography, Container } from '@mui/material';

const RetaurantManagerHome = () => {
  return (
    <Container>
      <Typography variant="h4">Welcome Restaurant Manager!</Typography>
      {/* Add restaurant manager-specific content here */}
    </Container>
  );
};

export default RetaurantManagerHome;
